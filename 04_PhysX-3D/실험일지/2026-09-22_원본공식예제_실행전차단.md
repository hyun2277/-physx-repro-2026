# 2026-09-22 패치 없는 원본 공식 예제 실행 전 점검

## 요청 범위

고정 commit `4f54e750a309fe9cd9f20816916ecc0e8a9ae594`의 원본 `example.py` 1건을 실행하려 했으나, 실행 전 필수 조건이 충족되지 않아 중단했다. 모델 다운로드·설치·GPU 실행·출력 생성은 하지 않았다.

## 점검 결과

| 점검 | 결과 | 근거 |
|---|---|---|
| 원본 HEAD | 통과 | `sources/physx-4f54e750a309` HEAD가 `4f54e750a309fe9cd9f20816916ecc0e8a9ae594` |
| 원본 작업 트리 | **차단** | `git status --porcelain`에 추적되지 않은 `pretrain/`이 있음. source 파일의 tracked diff는 없지만 전체 사본은 clean하지 않음 |
| 모델/체크포인트 | 로컬 존재 | `pretrain/diffusion/ckpts_new`와 `pretrain/trellis/ckpts`에 관련 `.pt`/`.safetensors`가 있음. 대용량 파일은 Git에 추가하지 않음 |
| 기본 환경 패키지 | 로컬 설치 확인 | `envs/physxgen`에서 torch, clip, PIL, trimesh, imageio 모듈 탐색 성공 |
| 입력 | 확인 | 원본 기본 입력 `example/table.png`, 483,075 bytes, SHA256 `67ce4c07693c8814dbdb068c7184b3b6341dc8fd7a595cdaef314d0fb1c2b388` |
| GPU | **차단** | `nvidia-smi --query-gpu=...`가 “couldn't communicate with the NVIDIA driver”로 실패, exit 9 |

## 원본 예제 기본 동작

원본 `example.py`는 `--condpath` 기본값 `./example/table.png`, `--savepath` 기본값 `./outputs_vis`, `--question` 기본값 `Cushioned seat surface`, `--question_type` 기본값 `0`을 사용한다. 모델 경로는 코드의 `pretrain/diffusion`이다. 이번에는 인자 해석·pipeline 실행·출력 폴더 생성을 시작하지 않았다.

## 실행 판정

새 timestamp 출력 폴더, stdout/stderr, 종료 코드, GPU 사용량, 모델 hash 기록은 **실행 자체가 시작되지 않았으므로 생성하지 않았다**. 원본 source·기존 patch 작업 사본·기존 결과물은 변경하지 않았다.

재개 전에 원본 사본을 추적/미추적 파일까지 깨끗하게 만들고, 기존 시스템 설정을 바꾸지 않은 상태에서 사용 가능한 GPU를 다시 확인해야 한다. GPU가 없거나 드라이버 통신이 실패하면 같은 실행을 반복하지 않는다.

이번 기록은 논문 정량평가나 논문 수치 재현 성공을 의미하지 않으며, 공식 예제 실행 전 차단 결과만 남긴다.
