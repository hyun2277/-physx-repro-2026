# 2026-09-22 원본 공식 table 예제 사전점검 재시도

## 결과

GPU 1만 사용하도록 실제 실행 전 `CUDA_VISIBLE_DEVICES=1`을 설정하고, 사전 점검에서 중단했다. `nvidia-smi`는 이 점검에서 한 번 실행했으며 통신 실패 메시지와 종료 코드 `9`를 남겼다. PyTorch는 `2.7.1+cu128`이지만 `cuda_available=False`, `device_count=0`이고 CUDA tensor 검산도 성공하지 않았다. 두 조건 중 하나라도 실패하면 예제를 실행하지 않는 규칙에 따라 원본 `example.py`는 실행하지 않았다.

실행 폴더: `PHYSx/logs/official-example-original-20260922/20260922T044403Z/`

## 보존 및 사전 확인

- 원본 HEAD는 `4f54e750a309fe9cd9f20816916ecc0e8a9ae594`와 일치했다.
- tracked source diff 종료 코드는 `0`이었다.
- `?? pretrain/`은 기존 체크포인트로 확인했으며 삭제·수정·`git clean`하지 않았다.
- 입력 SHA256과 모델 파일별 경로·크기·SHA256은 실행 폴더의 `input_sha256.txt`, `model_inventory.txt`에 기록했다. 모델·결과·캐시는 Git에 추가하지 않았다.
- 새 설치·다운로드·드라이버/CUDA 변경·시스템 설정 변경은 없었다.

이 기록은 원본 공식 예제 실행이나 논문 정량평가 성공을 의미하지 않는다. GPU 통신과 PyTorch CUDA 사용 가능성이 다시 확인되기 전에는 재실행하지 않는다.
