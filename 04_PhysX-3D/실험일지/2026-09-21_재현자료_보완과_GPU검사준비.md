# 2026-09-21 — 재현 자료 보완과 다음 GPU 검사 준비

## 목적과 상태

Windows에서 `b9fb49b` 게시본을 검토하며 발견한 설치 계획·CLIP patch·설치 실행기 입력 누락을 Linux 원본과 대조해 보완했다. **자료 검사 4종은 exit0으로 완료됐다. 설치·빌드·모델 다운로드·GPU 실행/사용량 측정은 미실행이다.** 현재 CUDA 및 기본 Python 패키지를 재설치하지 않았다.

## 환경과 입력

- 시각: 2026-09-21, Asia/Seoul(+09:00). 작업 루트 `/home/minsujo/Desktop/SH/PHYSx`.
- 기록 저장소: `repro-records`, `main`; 시작 HEAD/원격 main은 `b9fb49be22e1d12ed31e09341b47daff58ca7fa7`.
- 15:52:17 fetch exit0: 원격의 추가 변경 없음. 기존 `ebd8d89`, `4052f29` 이력 보존.
- 기존 설치 기록: Toolkit 12.8.1/NVCC 12.8.93, Python 3.10.21, torch 2.7.1+cu128, torchvision 0.22.1+cu128. 이 문서의 버전은 과거 완료 기록을 근거로 하며 이번에 GPU나 모델 패키지를 import해 다시 측정하지 않았다.
- 이번 원본 사본 57개/5,231,294바이트와 새 안내·검사 코드의 경로·크기·SHA256은 [materials-manifest.json](../재현자료/2026-09-21_자료보완/materials-manifest.json)에 있다.

## 명령과 실제 결과

원본 실행 기록: `PHYSx/logs/repro-materials-20260921/<이름>-20260921T160605.../`. 각 실행에 분리된 stdout/stderr/command/result를 남겼다. 공개한 선별본은 [validation](../재현자료/2026-09-21_자료보완/validation/README.md)에 있다.

| 실행 | 시각(+09:00) | exit | 결과 | 명령/증거 |
| --- | --- | --- | --- | --- |
| verify-materials | 16:06:05.567–05.616 | 0 | manifest 65개 크기/해시 통과, foundation 필수 10개 존재 및 7개 입력 해시 통과 | [command](../재현자료/2026-09-21_자료보완/validation/verify-materials.command.json), [stdout](../재현자료/2026-09-21_자료보완/validation/verify-materials.stdout.log) |
| static-audit | 16:06:05.580–05.682 | 0 | 사본 57개=원본, Python 11개 AST, 기존 34개 격리 hash, foundation 7/CUDA 9 입력 hash, 링크·공개 범위 확인 | [command](../재현자료/2026-09-21_자료보완/validation/static-audit.command.json), [stdout](../재현자료/2026-09-21_자료보완/validation/static-audit.stdout.log) |
| gpu-plan | 16:06:05.591–05.621 | 0 | 지정 Python `-I -B -S`로 계획 JSON만 출력, `executed=false` | [command](../재현자료/2026-09-21_자료보완/validation/gpu-plan.command.json), [stdout](../재현자료/2026-09-21_자료보완/validation/gpu-plan.stdout.log) |
| gpu-plan-mock-tests | 16:06:05.599–05.677 | 0 | 모의 검사 10개 통과; 실제 GPU·NVIDIA 명령·torch 실행 없음 | [command](../재현자료/2026-09-21_자료보완/validation/gpu-plan-mock-tests.command.json), [stderr](../재현자료/2026-09-21_자료보완/validation/gpu-plan-mock-tests.stderr.log) |

정적 검사 출력의 변경 파일 수 68개는 그 검사 시점의 값이다. 이후 이 일지와 선별 검증 로그가 추가되므로 최종 커밋 파일 수와 동일하지 않다.

## 실패·원인·조치·재실행 결과

- 증상: 게시본에 최신 Linux `installation-plan.md`와 CLIP patch가 없고, foundation의 `before.json`, sparse 후보 lock, pip 초기/ utils3d 보고서 4개가 빠졌다.
- 원인(확인): 앞선 증거 선별은 설치 결과 중심이었고 실행기가 읽는 입력 전체를 포함하지 않았다. sparse lock은 설치 대상이 아니어도 입력 hash 확인에 필요했다.
- 수정: 원래 `logs/...` 구조의 사본, 자료 manifest/읽기 전용 검사기, 현재 상태와 실행 순서, Windows 줄바꿈 보존 설정을 새 `재현자료`에 추가했다. 옛 계획·로그는 바이트 그대로 보존했다.
- 추가 발견: CUDA 실행기는 고정 로그 파일 11개 외에 당시 Conda 캐시 metadata 21개를 읽었다. 캐시 본체를 올리지 않고 경로·크기·실측 SHA256을 기록했으며, 이 자료만으로 독립 재설치가 가능하다고 표현하지 않았다.
- GPU 준비 코드 검토: 프로세스 시작 실패와 연산 후 사용량 조회 실패를 구분하도록 기록을 보완했다. 사용량에 총/사용 메모리·사용률·온도·전력 및 실제 조회 시각을 포함하도록 준비했다. 실제 실행 전 문제를 정적 검토로 수정한 것이며, GPU 실행 실패·재실행이 발생한 것은 아니다.
- 재확인 결과: 위 4종 검사 exit0. 설치 실행기를 재실행해 확인하지 않았다. 현재 환경에서 재실행하면 기존 실패 기록을 덮을 수 있다는 점을 안내에 명시했다.
- 커밋 전 whitespace 검사: 전체 `git diff --cached --check`는 exit2였다. 원인은 원본 그대로 보존한 CLIP/PhysX upstream 검토문 3개의 기존 줄 끝 공백·끝 빈 줄이다. 원본 SHA256 보존을 위해 이를 수정하지 않았다. 새로 작성한 파일만 대상으로 한 같은 검사는 exit0이었다. stdout/stderr/명령/exit은 `PHYSx/logs/repro-materials-20260921/staged-whitespace-*`에 남겼다. 해시·입력·모의 검사 실패나 GPU 실행 실패가 아니다.

## 검증 증거와 한계

[안내서](../재현자료/2026-09-21_자료보완/README.md)에서 과거 계획·현재 설치 완료·다음 미실행 단계를 구분한다. 최신 계획서의 “미설치”는 작성 당시 상태다. 옛 `artifact-sha256.json`의 일부 해시는 보완 전 값이며 이번 manifest로 대조한다.

CLIP의 custom 환경 변수만 설정해서는 다운로드 경로가 바뀌지 않는다. 두 `clip.load` 호출의 patch와 `PHYSx/cache/clip` 지정이 함께 필요하다. patch는 현재 미적용이며 모델 다운로드·파일 해시 검증도 수행하지 않았다. GPU 준비 코드는 새 별도 launcher이며 기존 설치 launcher의 GPU 숨김 동작은 변경하지 않았다.

모의 검사는 namespace/드라이버 실제 동작, shared library load, CUDA 수치 결과를 증명하지 않는다. Windows에서도 검증기를 실제 실행한 결과는 아직 없다. 환경·캐시·모델·데이터·대형 결과·인증정보는 Git에 포함하지 않았다. 다음 작업은 **사용할 GPU 한 개를 정한 뒤 별도 승인하에 기본 PyTorch GPU 검사를 실행하고 사용량을 기록하는 것**이다.

## 기록 공개 상태

이번 파일은 모두 `04_PhysX-3D/재현자료/` 및 이 새 실험일지에 한정한다. 논문 1~3, 최상위 README, 기존 다른 사람 파일을 수정하지 않는다. 파일별 명시적 stage와 최종 범위 검토 후 일반 push하며, 게시 결과는 실제 원격 main 대조 후 이 일지와 `PHYSx/logs/repro-materials-20260921/`에 추가 기록한다. 인증은 저장소 전용 hyun2277 메모리 캐시를 사용하며 선배의 저장 인증·전역 설정은 변경하지 않는다.
