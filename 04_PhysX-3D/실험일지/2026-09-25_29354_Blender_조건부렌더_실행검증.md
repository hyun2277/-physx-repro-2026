# 29354 Blender 조건부 렌더 실행 검증

## 범위

29354의 기존 `model_tex.obj` 한 표본으로 공식 `render_cond.py`의 `_render_cond` 경로를 한 번 실행했다. 논문 전체 평가, 30-view 조건, 형상·재질 정확도 검증은 수행하지 않았다. 원본 소스와 기존 staging은 수정하지 않았다.

## 사전 확인

- 고정 source HEAD: `4f54e750a309fe9cd9f20816916ecc0e8a9ae594`.
- 공식 코드가 요구하는 Blender: `blender-3.0.1-linux-x64`이며 기존 `/tmp/blender-3.0.1-linux-x64/blender`가 없어 이전 preflight가 중단됐다.
- 공식 다운로드: `https://download.blender.org/release/Blender3.0/blender-3.0.1-linux-x64.tar.xz`.
- 아카이브 크기: `186027304` bytes. 공식 SHA256: `4f17aa3d10ed6e13e6a75479f1a506f58998b8c007812a0886d9254c953e2ae5`; 실제 hash가 일치했다.
- PHYSx 내부 portable binary `Blender 3.0.1`, build hash `dc2d18018171`의 `--background --version` 실행이 성공했다. 시스템 설치, sudo, apt, 라이브러리 변경은 하지 않았다.

## 실행

실행 ID는 `20260925T185825Z`이다. `render_cond.py` 원본은 수정하지 않고, PHYSx 내부 wrapper가 고정 source 모듈을 import한 뒤 Blender 경로만 portable binary로 주입했다. 새 staging에 `model_tex.obj`와 공식 CSV 필드의 29354 한 행만 배치했으며 `CUDA_VISIBLE_DEVICES=''`, 24 views로 CPU 경로를 실행했다.

- staging: `/home/minsujo/Desktop/SH/PHYSx/staging/render-cond-29354-portable-20260925T185825Z/`
- 로그: `/home/minsujo/Desktop/SH/PHYSx/logs/render-cond-29354-portable/20260925T185825Z/`
- 종료 코드: `0`
- 출력 검증 종료 코드: `0`
- 검증: `transforms.json` 존재, PNG 24개 모두 실제 Pillow decode 성공(각 1024x1024), 24개 4x4 transform matrix가 유한함.

공식 기본값은 24 views이고 카메라 offset, radius, FOV가 비고정 난수로 정해진다. 따라서 이번 결과를 논문의 고정 30-view 외관 평가 조건이나 논문 정량평가 결과로 해석할 수 없다.

## 증거

실행 명령, stdout/stderr, 시각, 종료 코드, 입력·복사본 hash와 Blender version은 위 로그에 보존했다. archive·binary·staging 이미지와 transforms는 Git에 추가하지 않았다. hash 목록은 동반 evidence 파일에 기록했다.
