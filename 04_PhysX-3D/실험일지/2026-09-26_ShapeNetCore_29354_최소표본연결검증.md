# ShapeNetCore 29354 최소 표본 연결 검증

- 조사일: 2026-09-26 (Asia/Seoul)
- 목적: PhysXNet 표본 `29354`의 ShapeNet 후보 원본 연결만 확인
- 범위 제외: PartNet-archive 다운로드, 전체 PhysXNet/ShapeNetCore, 전체 압축 해제, 전처리, 추론, 학습, 전체 평가

## 접근과 다운로드

`envs/physxgen/bin/hf auth whoami`가 `hyun2277`을 반환했다. ShapeNetCore gated 저장소에서 `04379243.zip` 파일 하나만 `/home/minsujo/Desktop/SH/PHYSx/data/shapenetcore/raw/`에 받았다.

- 예상 크기: `1,666,983,898` bytes
- 실제 크기: `1,666,983,898` bytes
- ZIP SHA256: `625701ed0a2de08cc28bce6fd4f3643c67ed573c6abcbba5f0cbbb5e810d0503`
- PartNet-archive에는 접근·다운로드하지 않았다.

ZIP 전체를 풀지 않고 중앙 목록만 읽었다. 후보 `db406d9b2a94bce5622d7484764b58f` 아래 9개 member가 존재했다. 그중 연결에 필요한 OBJ·MTL·texture 3개만 다음 위치로 추출했다.

`/home/minsujo/Desktop/SH/PHYSx/data/shapenetcore/minimal/29354/`

- `models/model_normalized.obj` — 12,044 bytes; SHA256 `d538696f9ac131cf3b36134d1ada25426701e85375c03daff26561e9c4933a9a`
- `models/model_normalized.mtl` — 452 bytes; SHA256 `0eb2c76763251d7d00dff73d0f130db6a482a7bd2adfd48896f8454c74ac2ee5`
- `images/texture0.jpg` — 25,958 bytes; SHA256 `37d97e4c0897fc8c139bf128379cfb033f5f207cc1ecec746691a1d8664848a9`

OBJ의 `mtllib model_normalized.mtl` 참조와 MTL의 `map_Kd ../images/texture0.jpg` 참조가 추출된 상대 경로와 일치한다. texture는 JPEG 256×256으로 확인했다. 후보 아래의 JSON 및 binvox 파일은 이번 최소 연결 검증에 필요하지 않아 추출하지 않았다.

## 판정과 한계

`finalindex.json`의 기존 후보 문자열 `shapenet/04379243/db406d9b2a94bce5622d7484764b58f`와 실제 ShapeNetCore 원본 OBJ·MTL·texture가 연결됨을 확인했다. 이는 **29354 한 표본의 texture-source 연결 검증 성공**이다.

이는 논문 전체 재현이나 논문 동일 정량평가 성공을 뜻하지 않는다. PartNet 원본 연결, 공식 test conditioning image/camera/transforms, metric-ready GT map/mask, 질문·관절 대응 manifest 및 평가 절차는 별도 확인이 필요하다.

`7271`은 ShapeNet 매핑이 없으므로 조사하지 않았고, 임의 매핑·회색 texture 대체·추론·평가를 하지 않았다. 데이터 ZIP, 추출 파일, 캐시, 인증정보는 Git에 추가하지 않는다.
