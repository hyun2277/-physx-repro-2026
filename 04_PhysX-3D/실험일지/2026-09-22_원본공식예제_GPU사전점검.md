# 2026-09-22 원본 공식 예제 GPU 사전점검

## 결과

고정 원본에서 공식 table 예제를 실행하려 했으나 사전 점검에서 중단했다. `nvidia-smi --query-gpu=index,uuid,name,memory.used,memory.total,utilization.gpu --format=csv,noheader,nounits`를 새 실행 폴더에서 실행했지만 stdout이 “couldn't communicate with the NVIDIA driver”였고 종료 코드는 `9`였다. Python 환경 점검에서도 `torch=2.7.1+cu128`, `cuda_available=False`였다. 따라서 GPU 1의 UUID·사용량·production 프로세스 부재를 확인할 수 없어 `CUDA_VISIBLE_DEVICES=1` 예제를 실행하지 않았다. 새 설치·다운로드·시스템 설정 변경은 없었다.

실행별 로그: `PHYSx/logs/official-example-original-20260922/20260922T042324Z/`

## 원본과 준비물 점검

- source HEAD: `4f54e750a309fe9cd9f20816916ecc0e8a9ae594` (요구한 고정 commit과 일치)
- tracked diff 종료 코드: `0`
- 상태의 `?? pretrain/`은 기존 모델 체크포인트이며 삭제하거나 `git clean`하지 않았다.
- 입력 `example/table.png` SHA256: `67ce4c07693c8814dbdb068c7184b3b6341dc8fd7a595cdaef314d0fb1c2b388`
- 모델 파일은 경로·크기·SHA256을 `model_inventory.txt`에 기록했다. 모델과 캐시는 Git에 추가하지 않았다.
- Python, CLIP, trimesh, imageio import 점검은 성공했지만 CUDA 통신 실패 때문에 실행 준비 완료로 판정하지 않았다.

이 기록은 원본 예제 실행 성공이나 논문 재현을 의미하지 않는다. GPU 통신과 GPU 1의 사용 가능 상태가 별도로 확인된 뒤에만 예제를 다시 검토한다.
