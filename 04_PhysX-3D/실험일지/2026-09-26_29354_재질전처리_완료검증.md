# 29354 재질 전처리 작업 묶음 완료 검증

- 기준 commit: `4b95b35`
- 공식 source HEAD: `4f54e750a309fe9cd9f20816916ecc0e8a9ae594`
- 범위: 기존 merge 산출물 재사용, `rtree` 최소 설치, 공식 retrieval 29354 1건, CPU 검증
- 수행하지 않음: merge 재실행, PartNet/ShapeNet 재다운로드, ZIP 변경, Blender/render, voxel·feature·latent, GT, 추론, 학습, 전체 평가

## 실패 원인과 의존성 보완

기존 로그 `/home/minsujo/Desktop/SH/PHYSx/logs/retrieval-29354/20260925T181057Z/stderr.log`의 실제 원인은 다음이었다.

```text
trimesh.proximity -> source_mesh.nearest.on_surface(...)
ModuleNotFoundError: No module named 'rtree'
```

기존 `envs/physxgen`의 `trimesh`는 4.6.8, Pillow는 11.2.1이었고 `rtree`는 없었다. Python 3.10용 PyPI 바이너리 `Rtree==1.4.0` wheel만 PHYSx 내부 캐시에 내려받아 설치했다.

- wheel: `cache/rtree-20260926/wheels/rtree-1.4.0-py3-none-manylinux2014_x86_64.manylinux_2_17_x86_64.whl`
- wheel SHA256: `27e4a6d617d63dcb82fcd4c2856134b8a3741bd1af3b1a0d98e886054f394da5`
- 설치 대상: `/home/minsujo/Desktop/SH/PHYSx/envs/physxgen`
- 설치 전후 변경: `rtree`만 추가 (`pip-freeze-before.txt`/`pip-freeze-after.txt`)
- `pip check`: `No broken requirements found`
- 작은 CPU `trimesh.nearest.on_surface` 실행: 종료 코드 0, 유한한 최근접점·거리·face id 확인

설치 로그와 버전/hash는 `/home/minsujo/Desktop/SH/PHYSx/logs/retrieval-29354/20260926T033700Z/`에 있다.

## retrieval 실행과 보정 검증

실행기는 공식 원본을 수정하지 않고 다음을 검사하도록 보완했다.

- source HEAD 및 관련 tracked 변경 검사(기존 `__pycache__/*.pyc`, `pretrain/` 보존 항목 허용)
- merge model과 ShapeNet 입력 hash 확인 및 복사본 hash 기록
- 공식 `finalindex.json`의 29354 단일 매핑 검증
- 동시 실행 lock, 고유 timestamp staging, 단계별 로그·시작/종료 시각·종료 코드
- import/CPU nearest preflight 및 산출물 검증 실패 기록

첫 보완 실행은 pretrain 상태 접두사를 잘못 해석한 검사기 버그로 사전 중단됐다(`20260925T183845Z`, 종료 2). 이 실행은 retrieval을 시작하지 않았다. 검사를 고친 뒤 한 번만 공식 retrieval을 실행했다.

- 실행 staging: `/home/minsujo/Desktop/SH/PHYSx/staging/retrieval-29354-20260925T183908Z/`
- 실행 로그: `/home/minsujo/Desktop/SH/PHYSx/logs/retrieval-29354/20260925T183908Z/`
- 공식 retrieval 종료 코드: `0`
- stderr 기록: `29354 mapping`
- 최종 출력 검증: 보정 검증 스크립트 종료 코드 `0`

생성 파일:

- `phy_dataset/29354/model_tex.obj` — 1,106,789 bytes, SHA256 `e58a15b42169119b66da9db751e25ed00c0c57d41de3db83d022538617532633`
- `phy_dataset/29354/material.mtl` — 198 bytes, SHA256 `0d90fe7d688a6da0b82b0f8053231a1c8986de34aa839f8113ed53a610c09227`
- `phy_dataset/29354/material_0.png` — 102,720 bytes, SHA256 `df6894182ab1a3754524c12f7946ad4d4439452e1dcfaaf6bd97cf5173eda06d`

검증 결과:

- OBJ의 `mtllib material.mtl`과 MTL의 `map_Kd material_0.png` 참조가 실제 파일로 열림
- 출력 texture PNG 실제 디코딩 성공: 512×512 RGBA
- UV 8,902개, 모두 유한
- 입력 `model.obj`와 출력의 vertex 8,902개·face 17,904개가 일치; trimesh 재로딩 후에도 형상 보존 통과
- 출력 texture 불투명 픽셀의 ShapeNet 원본 texture 색 집합 일치율 `0.999878`
- 출력은 원본 256×256 JPEG의 바이트 복사본이 아니라 512×512 PNG atlas/re-encode 결과로 판정
- 실제 `map_Kd`와 ShapeNet 색 집합이 확인되어 회색 1×1 fallback 분기는 사용되지 않음

초기 검증기는 원본과 출력 이미지 배열의 크기·바이트 동일성을 요구해 실패로 기록했다(`output_verification.exit_code.txt=1`). 이는 retrieval 실패가 아니라 검증 기준의 false negative였으며, 이후 atlas/re-encode를 구분하는 CPU 검증기를 실행해 통과시켰다. 초기 실패와 보정 결과 모두 보존했다.

## 판정과 한계

이번 작업의 판정은 **29354 재질 asset 생성 및 연결 검증 성공**이다. 공식 retrieval 코드가 `model.obj`에 ShapeNet texture를 연결해 `model_tex.obj`와 참조 MTL/texture를 만들었고, geometry·UV·파일 연결을 확인했다.

확인하지 않은 사항은 texture/재질의 시각적 품질, 물리적 재질 정확도, 카메라 렌더, GT, 논문 정량평가다. 7271은 ShapeNet 매핑이 없어 계속 제외한다.

데이터·mesh·texture·staging 산출물·CLIP/rtree wheel·캐시·인증정보는 Git에 추가하지 않았다.
