# PhysX-3D 실험일지 — 공식 예제 준비 2: Kaolin·렌더러

- 일시: 2026-09-21 18:11–18:14 KST. 앞 단계 `464c38f` 게시 확인 후 진행.
- 목적: 설치 성공과 실제 CUDA forward/backward 성공을 분리해 확인한다.
- 환경: Python3.10.21, torch2.7.1+cu128/torchvision0.22.1+cu128 유지, 로컬 Toolkit12.8.1·GCC13.3.0.
- GPU: 호스트1번/UUID `GPU-843dced4-ee97-dbb8-36c9-343fe13b7647`/minor1/PCI02:00.0. 각 실행 직전 호스트 계산 프로세스가 없음을 확인. 격리 내부0번 UUID/PCI 대조. 세 검사는 순차 실행했다.
- 경계: 네트워크 차단, 환경/Toolkit 읽기 전용, GPU 한 장, 캐시/임시/컴파일 출력은 PHYSx 내부. Kaolin 재설치 없음.

## 실행과 결과

| 대상 | 최소 검사 | 결과 |
|---|---|---|
| Kaolin0.18.0 | 3·4개 점군 native sided distance/index, Chamfer, backward | 종료0. 거리 CPU오차0, gradient 최대절대오차 약3.97e-8 |
| 고정 mip Gaussian rasterizer | +Z 카메라, Gaussian1개, 16×16 영상·backward | 종료0. radius3, 유효pixel16, 단일 색 alpha 일치오차0. 검사한 gradient 유한 |
| nvdiffrast0.3.3 | CUDA plugin JIT, 삼각형1개, 16×16 raster/interpolate·backward | 종료0. 유효pixel72, CPU barycentric 최대절대오차 약3.97e-8. vertex/attribute gradient 유한·비영 |

Kaolin·Gaussian·nvdiffrast 내부 검사 시간은 각각 약7.05·2.20·54.39초다. 외부 격리/조회 시간은 별도이며 전체 시각은 실행 기록에 있다. nvdiffrast는 설치된 Python wheel만으로 검증한 것이 아니라 `PHYSx/build/torch-extensions`에서 CUDA 플러그인을 생성하고 실제 연산했다. GPU가 보이는 JIT 경로를 사용했으며 OpenGL context는 사용하지 않았다.

이번 세 검사에는 실패나 재시도가 없었다. 앞 단계에서 보완한 Conda CUDA include/link 경로와 호스트/격리 내부 GPU 번호 대조를 유지했다. 실행 후 실제 로드된 확장·CUDA 라이브러리 경로와 GPU 전/중/후 표본도 기록했다.

## 기록과 한계

[검사 코드·실행별 stdout/stderr/종료코드](../재현자료/2026-09-21_공식예제진행/02_renderer검사/), [검사별 수치](../재현자료/2026-09-21_공식예제진행/02_renderer검사/summary.json), [GPU 표본](../재현자료/2026-09-21_공식예제진행/02_renderer검사/gpu-samples.json), [원본 위치·크기·해시](../재현자료/2026-09-21_공식예제진행/02_renderer검사/evidence-manifest.json).

대표 명령은 `workspace_runner.py --execute --profile gpu --label <case>-smoke --gpu-uuid <위 UUID> --gpu-index 1 --device-minor 1 -- <env Python> -I -B -S check_extensions.py --case kaolin|gaussian|nvdiffrast --execute --gpu-uuid <위 UUID>`이며 전체 절대경로 argv를 증거에 보존했다.

이는 작은 native 연산 검증이다. FlexiCubes 전체 메시 추출, 공식 Gaussian 장면, GLB 전체 후처리, 모델 예측 정확도를 보장하지 않는다. FlashAttention 빌드와 고정 모델 다운로드는 아직 진행 중이다. 다음은 attention 실제 함수 검사와 다운로드 무결성 확인 후 공식 table 예제 1개 실행이다.
