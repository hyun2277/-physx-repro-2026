# 29354 inference source clean 검사 보완

`run_inference_29354_preflight.sh`의 source 검사에서 staged와 unstaged tracked 경로를 각각 읽고 합쳐 중복 제거하도록 보완했다. `trellis/(.../)?__pycache__/*.pyc` 경로만 자동 생성 예외로 허용하며, 그 밖의 `.py`, 설정, 모델 코드, 다른 경로의 `__pycache__` 변경은 중단 사유다. untracked `pretrain/`은 계속 보호하고 수정하지 않는다.

2026-09-25T19:07Z 사전검사 결과: staged tracked 변경 0개, unstaged 변경 70개이며 70개 전부 `trellis/**/__pycache__/*.pyc`였다. 남은 tracked source 변경은 0개다. `example.py`, GPU, 추론은 실행하지 않았다. `bash -n`과 허용 `.pyc`/금지 경로 mock 검사를 통과했다.
