# 공식 merge_property.py 29354 한 표본 실행 검증

- 실행일: 2026-09-26 (Asia/Seoul)
- 원본 commit: `4f54e750a309fe9cd9f20816916ecc0e8a9ae594`
- 목적: PhysXNet 29354의 JSON·6개 부품 OBJ에서 공식 merge 산출물 생성 가능 여부 확인
- 실행하지 않음: PartNet/ShapeNet 다운로드, 기존 ZIP·최소 추출물 변경, retrieval, `model_tex.obj`, Blender, GT 생성, 모델 추론, 학습, 전체 평가

## 실행 경로와 보호

29354 입력만 새 staging에 복사했다.

- staging: `/home/minsujo/Desktop/SH/PHYSx/staging/merge-29354-20260925T180423Z/`
- 입력 JSON: `physxnet/finaljson/29354.json`
- 입력 OBJ: `physxnet/partseg/29354/objs/0.obj`–`5.obj`
- 출력: `phy_dataset/29354/`
- 로그: `/home/minsujo/Desktop/SH/PHYSx/logs/merge-29354/20260925T180423Z/`

기존 캐시 `/home/minsujo/Desktop/SH/PHYSx/cache/clip/ViT-L-14.pt`를 staging HOME의 심볼릭 링크로 사용했다. 새 다운로드는 하지 않았다.

GPU 사전 확인은 GPU 1만 선택하도록 수행했다.

- GPU 1 UUID: `GPU-843dced4-ee97-dbb8-36c9-343fe13b7647`
- 사전 compute process: 없음
- 실행 환경: `CUDA_VISIBLE_DEVICES=1`
- 명령: `/home/minsujo/Desktop/SH/PHYSx/envs/physxgen/bin/python /home/minsujo/Desktop/SH/PHYSx/sources/physx-4f54e750a309/dataset_toolkits/merge_property.py --index 0 --range 1 --datapath <staging>/physxnet`
- CLIP cache: 932,768,134 bytes, SHA256 `b8cca3fd41ae0c99ba7e8951adf17d267cdb84cd88be6f7c2e0eca1737a03836`

첫 샌드박스 시도(`20260925T180404Z`)는 `nvidia-smi`가 드라이버와 통신하지 못해 GPU UUID를 확인하지 못하고 보호 단계에서 종료 코드 2로 중단됐다. 같은 명령을 무근거로 반복하지 않고 원인을 로그로 확인한 뒤, 정상 GPU 장치가 보이는 승인된 실행 경계에서 1회 실행했다.

## 결과

정상 GPU 경계 실행은 **종료 코드 0**이며 stderr 로그에도 `success: 29354`가 기록됐다.

| 생성 파일 | 크기 | 기본 형태 |
|---|---:|---|
| `model.obj` | 622,283 bytes | vertex 8,902 / face 17,904 |
| `clip.npy` | 73,856 bytes | shape `(6, 4, 768)`, `float32` |
| `clip_ind_new.npy` | 35,736 bytes | shape `(8902,)`, `float32` |
| `otherproperty.npy` | 800 bytes | shape `(6, 14)`, `float64` |

생성물별 SHA256과 원본 입력 hash는 [hash 목록](../자료확보/evidence/2026-09-26-merge-29354-output-hashes.txt)에 기록했다. 이는 파일 생성·기본 형태 검증이며, 논문 지표나 수치 정확성을 뜻하지 않는다.

## 다음 단계

공식 merge 단계는 29354 한 표본에서 통과했다. 다음 단계는 별도 승인 후 retrieval 입력 경로를 staging에 맞춰 검토하는 것이며, 이번 실행에서는 `retrieval_texture_example.py`, `model_tex.obj`, Blender/render, GT, 추론을 실행하지 않았다. 7271은 ShapeNet 매핑이 없어 계속 제외한다.

데이터·mesh·texture·CLIP 가중치·캐시·staging 출력은 Git에 추가하지 않았다. 커밋 대상은 04_PhysX-3D 내부의 실행 스크립트·일지·hash 목록뿐이다.
