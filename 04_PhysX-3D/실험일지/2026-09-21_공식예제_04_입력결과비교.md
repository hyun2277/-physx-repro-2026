# PhysX-3D 실험일지 — 기존 입력과 저장 결과의 시점 비교

- 일시: 2026-09-21 21:52–21:53 KST.
- 목적: 이미 생성된 결과와 입력을 나란히 관찰한다. 모델 추론·다운로드·설치·확장 빌드·추가 GPU 실험은 수행하지 않았다.
- 입력: 고정 `sources/physx-4f54e750a309/example/table.png` 및 기존 `outputs/official-example/table-20260921T122033Z-60b5940050/pretrain/diffusion/rgb.mp4`.
- 환경/방법: 기존 Python3.10 환경의 Pillow11.2.1·imageio-ffmpeg0.6.0으로 CPU 디코딩/배치. 기존 workspace runner의 `build` 프로필은 GPU·네트워크 차단/환경 읽기 전용을 위해 사용했고, 실제 컴파일 명령은 실행하지 않았다.
- 실행 결과: 후보 프레임 추출과 최종 배치 모두 종료0. 실패·재시도 없음. stdout/stderr/시간/전체 명령은 [증거](../재현자료/2026-09-21_공식예제진행/04_입력결과비교/)에 보존했다.

## 프레임 선택

기존 전체30프레임 모음을 본 뒤0·1·13·14·28·29번을 원래512×512 크기로 비교했다. 상판을 낮게 내려다보는 각도와 얇은 타원 윤곽을 기준으로 **14번(0부터 번호, 약0.467초)**을 가장 가까운 근사 후보로 선택했다. 13번은 긴 축의 기울기가 비슷하지만 상판이 더 많이 드러난다. 이는 시각적 판단이며 카메라 자세의 정량 추정·최적 정합이나 정확한 시점 일치가 아니다. 입력 상판의 긴 축 기울기·원근·물체의 픽셀 크기도 완전히 같지 않다.

입력 비투명 영역 bbox는 `(326,443,756,605)`이고,512×512 crop `(285,268,797,780)` 안에 전부 포함됨을 확인했다. 잘라낸 영역은 완전히 투명한 바깥 여백뿐이다. 입력의 투명 영역은 검은 배경 위에 합성했고, 선택된 생성 프레임은 전체를 그대로 배치했다. 양쪽 모두 픽셀1:1이며 물체 회전·크기 변경·원근 변형·리터칭·부분 삭제는 하지 않았다. 선택 프레임 RGB 해시는 앞선 자동 출력 검사의14번 디코딩 해시와 동일했다.

## 관찰과 해석의 한계

입력에서는 중앙의 가느다란 긴 틈이 보이고, 선택한 생성 프레임에서는 더 넓은 타원형 구멍처럼 보인다. 이는 이 두 영상에 나타난 관찰 사실이다. **시점 차이는 틈/구멍의 보이는 폭과 형태에 영향을 줄 수 있으므로, 이 비교만으로 실제 형상 차이의 크기나 원인을 확정하지 않는다.** 모델·메시·렌더링 중 어느 단계가 원인인지, 시점 차이가 얼마나 기여하는지 분석하지 않았다. 형상을 맞추기 위한 재생성이나 추가 실험은 없었다.

## 결과

- 최종 이미지 전체 경로: `/home/minsujo/Desktop/SH/PHYSx/outputs/comparisons/table-input-vs-generated-frame14-20260921.png`
- 크기:1088×866,117,797 bytes.
- SHA256: `05c714a3705090cc7d4c63e144c995e24a6dc186acdcc0641fbf7bf85a507aca`.
- 이미지에 시점/배율 불일치, 사용 프레임, 관찰 사실, 원인 미확정과 배치 방법을 표시했다.
- PNG 원본은 로컬에 보존하고 Git에는 코드·필요한 작은 로그·경로/크기/해시만 기록한다. 기존 입력과 생성 결과는 변경하지 않았다.

대표 명령은 기존 격리 실행기 안에서 `make_comparison.py --mode candidates`, 이후 `make_comparison.py --mode comparison --frame 14`이다. 이는 완료한 CPU 자료 제작 기록이며 다음 실험 실행 안내가 아니다.
