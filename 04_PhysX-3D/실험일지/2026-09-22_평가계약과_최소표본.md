# 2026-09-22 평가 계약 회귀와 PhysXNet 최소 표본

## 범위

전체 test 평가·모델 추론·재학습·전체 ZIP 해제를 하지 않고, 평가 설정 검증의 일관성과 두 관찰 후보의 최소 파일 연결만 확인했다.

## 평가 코드 수정

- `evaluation/physx_eval/contracts.py`를 추가해 metric-level 필수 설정을 공통 검증한다.
- `mapping.validate_settings`와 `metrics.evaluate`가 같은 계약을 사용한다. `scale_l2`의 `representation`(`scalar`/`vector`)이 없으면 사전검사와 계산 모두 중단한다.
- CD/F-score의 거리·방향 축약·0.05 임계값 관련 필수 설정도 공통 계약에서 확인한다. 카메라·입력/GT 파일 연결 규칙은 기존 mapping 검증에 남긴다.
- `test_scale_representation_is_required_by_preflight` 회귀검사를 추가했다.

검증: 격리 CPU 합성 검산 run `logs/evaluation-cpu-20260921/20260921T160708482882Z-840ddad1`에서 54 tests, failures/errors 0, exit 0. `paper_equivalent=false`; 실제 데이터·GPU·모델은 사용하지 않았다.

## PhysXNet 최소 추출

공식 ZIP 전체 해시 `a4970a3936e8a1fc823d17b64d23b167d73997a13686011bc46f7b98774a7e75`, 16,306,354,206 bytes를 재확인하고 원본 test 순서의 index 0/1을 선택했다.

- test index 0 / source index 1000 / object `7271`: `group_info`의 non-root group 3개가 관찰된 `articulated_candidate`.
- test index 1 / source index 1001 / object `29354`: non-root group 0개가 관찰된 `fixed_candidate`.

두 후보는 논문의 공식 분류가 아니라 `group_info` 기반 관찰 후보다. 각 후보에 대해 JSON 1개, OBJ 6개, PNG 6개(각 13개)를 `/home/minsujo/Desktop/SH/PHYSx/data/physxnet-minimal/2026-09-22`에만 추출했다. 전체 ZIP은 해제하지 않았다. 파일별 해시는 실행 manifest에 있다.

JSON·OBJ·PNG가 같은 object ID의 `version_1/finaljson/<id>.json` 및 `version_1/partseg/<id>/{objs,imgs}` 경로로 연결되는 것은 확인했다. PNG가 평가 입력 이미지라는 점, 카메라 파라미터/뷰 대응, 텍스처, metric-ready 평가 GT라는 점은 이 최소 파일만으로 확정하지 않았다. 해당 상태는 `자료확보/evidence/2026-09-22-minimal-extraction.json`에 기록했다.

## 미검증 및 다음 단계

카메라 자료와 공식 입력·GT 대응을 별도 원본 경로에서 확인해야 하며, 그 전에는 정량 평가를 시작하지 않는다. 추출물은 Git에 추가하지 않는다.
