# 완료 출력 관찰 기록 — 2026-09-21

대상은 사용자 터미널 실행 `20260921T122021Z-415cd02be5`의 table 출력이다. 추론·설치·빌드·GPU 연산을 재실행하지 않았다. 기존 격리 실행기의 `build` 프로필은 여기서는 네트워크/GPU 차단과 환경 읽기 전용을 제공하는 용도로만 사용했으며, 실제 자식 명령은 `inspect_artifacts.py`를 통한 CPU 파일 읽기·디코딩·미리보기 작성이었다. 검사 코드에서 torch나 PhysX 모델을 import하지 않았다.

## 자동 검사와 이번 관찰의 구분

기존 `output-validation.json`은 passed/exit0/issues[]다. 실제 실행의 입력·소스·패치·모델 receipt·GPU UUID·시간·stdout을 출력 경로에 연결하고, 원시 텐서의 형상/유한성/인덱스, 영상 4개의 strict ffmpeg 디코딩과 실제 30프레임/30fps, GLB 메시/UV/embedded texture/scene transform을 확인했다. 파일이 존재하거나 종료 코드가 0이라는 사실만으로 통과시키는 검사가 아니다. 다만 신뢰하는 로컬 기록 간 일치 검사이며 변조 방지 증명은 아니다.

이번 관찰용 CPU 명령은 원본 출력 8개의 크기·SHA256이 기존 검증 기록과 여전히 일치함을 확인했다. 원시 PT를 다시 역직렬화하거나 기존 검증기를 재실행하지 않았다. 영상은 프레임 모음 생성을 위해 CPU로 읽었고, GLB는 CPU에서 UV 텍스처를 입힌 근사 정사영 미리보기 3개를 만들었다. 원본을 덮어쓰지 않고 새 폴더에 저장했다.

## 실제로 본 내용

- 입력 이미지: 밝은 타원형 상판, 중앙의 가느다란 긴 틈, 여러 T자 지지대가 보인다.
- `rgb.mp4`: 30개 프레임 전체를 한 장의 축소 이미지 모음으로 관찰했다. 검은 배경 위에 흰 타원형 상판과 여러 다리가 나타나고 관찰 각도가 변한다. 전체가 빈 영상이거나 단일 정지 프레임으로 반복되지는 않는다. 아래쪽 시점은 어둡게 보인다.
- **형상 차이**: 입력의 좁은 중앙 틈에 비해 출력 RGB와 GLB에는 큰 타원형 구멍이 보인다. 다리의 배치·윤곽도 입력과 완전히 일치한다고 확인하지 못했다. 파일 무결성 성공을 정확한 형상 복원 성공으로 표현하지 않는다. 원인 분리·정량 비교는 이번 범위에서 하지 않았다.
- `description.mp4`: 모든 프레임 모음에서 상판은 주로 노랑/주황, 다리는 청록/파랑으로 보인다. 질문 `wooden tabletop surface`에 대해 상판 쪽 반응이 큰 정성적 경향이다. 정답 마스크가 없으며 목재 재질 판단·grounding 정확도를 증명하지 않는다.
- `affordance.mp4`: 상판은 주로 빨강/주황, 다리는 녹색/청록이고 상판에 반점 형태의 변동이 보인다. 동작 가능성의 실제 정답 여부는 검사하지 않았다.
- `material.mp4`: 상판은 주로 파랑/청록, 다리는 빨강/주황으로 구분된다. 서로 다른 부위의 수치 차이를 시각화한 것으로 관찰했으며 재질명·물성 단위·값의 타당성은 판정하지 않았다.
- `texture.glb`: 저장된 4,533개 정점/6,098개 면과 1024×1024 RGB atlas를 읽었다. atlas 자체, 그리고 yaw 30/150/270도·elevation25도 CPU 미리보기에서 상판/구멍/다리가 보인다. 텍스처가 완전히 비어 있지는 않다. 미리보기는 PBR·안티앨리어싱을 구현하지 않은 근사 뷰이며 실제 3D 뷰어의 표시 품질과 같다고 보장하지 않는다.
- 원래 예제가 별도 PNG를 내보내지는 않았다. 이번 PNG들은 기존 영상/GLB의 **검토용 파생 파일**이다.

## 미확인과 한계

영상은 120개 프레임을 축소 모음으로 관찰했으며 실시간 플레이어 재생·시간적 부드러움이나 모든 픽셀을 정밀 판독하지 않았다. GLB의 대화형 회전/PBR 표시, watertightness, 연결성, 자기교차, 정답 메시와의 거리, 실척도, 텍스처 정합 정확도를 검사하지 않았다. 원시 텐서 수치 검사는 기존 자동 검사 보고서를 검토한 것이다. 물성/언어/affordance의 정답 정확도, 다중 관절의 parent/group 추론, 논문 지표, 반복 재현성, 다른 입력은 미검증이다. `num_group=1`이라 관절 영상·OBJ는 필요 없는 분기였고, 관절 mask 인덱싱 패치의 실행 검증도 아니다.

## 로컬 위치

- 원본 결과: `/home/minsujo/Desktop/SH/PHYSx/outputs/official-example/table-20260921T122033Z-60b5940050/pretrain/diffusion/`
- 검토 이미지와 해시: `/home/minsujo/Desktop/SH/PHYSx/logs/official-example-review-20260921/previews-20260921T122929Z-87522300/`
- CPU 검토 명령·stdout/stderr·종료0: `logs/official-example-20260921/runs/20260921T122927Z-completed-artifact-visual-review-96c2b97aae/`

영상·GLB·PT·파생 PNG는 Git에 올리지 않고 경로·크기·해시만 게시한다.
